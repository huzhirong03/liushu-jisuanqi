name: 获取开奖数据

# 触发条件
on:
  # 定时执行（北京时间 = UTC+8）
  schedule:
    # ===== 新澳门开奖时段 21:25-21:45 每2分钟 =====
    - cron: '25,27,29,31,33,35,37,39,41,43,45 13 * * *'  # UTC 13点 = 北京 21点
    
    # ===== 每天只在固定时间检查一次（非开奖时段）=====
    # 早上 9:00 检查一次（确保数据正常）
    - cron: '0 1 * * *'   # UTC 1点 = 北京 9点
    # 下午 18:00 检查一次（开奖前准备）
    - cron: '0 10 * * *'  # UTC 10点 = 北京 18点
  
  # 手动触发（随时可以手动运行）
  workflow_dispatch:
  
  # 推送代码时触发
  push:
    branches: [ main ]

jobs:
  fetch:
    runs-on: ubuntu-latest
    
    steps:
      # 检出代码
      - name: 检出代码
        uses: actions/checkout@v4
      
      # 设置 Node.js
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      # 获取开奖数据
      - name: 获取开奖数据
        run: node fetch-data.js
      
      # 提交更新
      - name: 提交更新
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data.json
          git diff --staged --quiet || git commit -m "🎰 更新开奖数据 $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M')"
          git push

